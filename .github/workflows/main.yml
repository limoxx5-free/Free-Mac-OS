name: macOS Interfaccia Grafica Reale

on:
  workflow_dispatch:

jobs:
  desktop-remoto:
    runs-on: macos-latest
    steps:
      - name: Attivare Schermo Nativo del Mac
        run: |
          echo "=== Configurazione Condivisione Schermo Apple ==="
          # Attiviamo il server VNC nativo di Apple usando comandi singoli digeriti dal sistema
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -configure -allowAccessFor -allUsers -privs -all
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -configure -clientopts -setvnclegacy yes -setvncpw PasswordSicura123
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -restart -agent
          echo "=== Schermo Apple Attivato ==="

      - name: Generare Indirizzo di Connessione Reale
        run: |
          echo "=== Creazione Tunnel di Rete Diretto ==="
          # Creiamo il tunnel usando il comando SSH nativo del Mac sulla porta grafica 5900
          ssh -R 80:localhost:5900 nokey@localhost.run > tunnel.log &
          
          echo "=== Attesa generazione indirizzo (15 secondi) ==="
          sleep 15
          
          echo "--------------------------------------------------------"
          echo " ECCO LE TUE CHIAVI DELLA FERRARI. CONNETTITI QUI:"
          # Estrae l'indirizzo reale generato dal tunnel di rete
          grep -oE "[a-zA-Z0-9.-]+\.lh\.lrun\.ch" tunnel.log || cat tunnel.log
          echo " Password dello Schermo: PasswordSicura123"
          echo "--------------------------------------------------------"

      - name: Mantieni in esecuzione per 6 ore
        run: |
          for i in {1..72}; do
            echo "Connessione grafica stabile. Minuti passati: $(( (i-1) * 5 ))"
            sleep 300
          done
