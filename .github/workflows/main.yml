name: Connessione macOS 14 Desktop via Tailscale (iPad OK)

on:
  workflow_dispatch:

jobs:
  macos-desktop:
    runs-on: macos-14
    
    steps:
      - name: Imposta password utente del Runner (Forzato)
        run: |
          sudo sysadminctl -deleteUserPassword runner
          sudo sysadminctl -resetPasswordFor runner -newPassword "MiaPassword123!"

      - name: Sblocca Firewall e attiva Screen Sharing
        run: |
          # 1. Configura il server VNC classico per evitare la schermata nera
          sudo defaults write /Library/Preferences/com.apple.RemoteManagement VNCAlwaysStartOnConsole -bool true
          sudo defaults write /Library/Preferences/com.apple.RemoteManagement VNCAccessPassword -string "MiaPassword123!"
          
          # 2. Disattiva il firewall di macOS per azzerare i timeout su iOS/iPadOS
          sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate off
          
          # 3. Forza l'attivazione della gestione remota
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
          -activate -configure -allowAccessFor -allUsers -privs -all -restart -agent

      - name: Installa e Connetti Tailscale
        run: |
          brew install tailscale
          sudo tailscaled > /dev/null 2>&1 &
          sleep 5
          
          # Connessione sicura usando la tua chiave racchiusa correttamente tra virgolette
          sudo tailscale up --authkey "tskey-auth-kR9WV4QAFT11CNTRL-mXJYy5CqyrhdoSnTaPV1shHgikUSTtTf" --accept-dns=false
          
          echo "============================================="
          echo "IL TUO IP TAILSCALE PER IL COLLEGAMENTO È:"
          tailscale ip -4
          echo "============================================="

      - name: Mantieni il workflow attivo
        run: |
          sleep 21600
